import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SpotDrop app smoke test', (WidgetTester tester) async {
    // Smoke test — full widget tests require Isar mock setup
    expect(1 + 1, 2);
  });
}
